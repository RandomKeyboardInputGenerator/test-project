import { SortLinkDirective } from './sort-link.directive';

describe('SortLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new SortLinkDirective();
    expect(directive).toBeTruthy();
  });
});
